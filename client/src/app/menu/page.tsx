import MenuPage from '@components/menu/menu-page/MenuPage';
import { type Metadata } from 'next';

export const metadata: Metadata = {
  title: 'טייקאווי ומשלוחים',
};

const Page = () => <MenuPage />;

export default Page;
