import ContactPage from '@components/contact/contact-page/ContactPage';
import { type Metadata } from 'next';

export const metadata: Metadata = {
  title: 'יצירת קשר',
};

const Page = () => <ContactPage />;

export default Page;
