import BusinessPage from '@components/business/business-page/BusinessPage';
import { type Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Cornicione & More',
};

const Page = () => <BusinessPage />;

export default Page;
